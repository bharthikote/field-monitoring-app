// Your laptop's LAN IP, so your phone (on the same WiFi) can reach the backend.
// If it stops connecting after switching networks, re-run `ipconfig` on the
// laptop and update this.
export const API_BASE_URL = 'http://192.168.0.5:4000';
